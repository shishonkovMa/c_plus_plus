#include <iostream>

using namespace std;
using std::cout;
using std::cin;

int formatText(const string& str, unsigned int pred)
{

    return;
}

int main()
{
    string str;
    unsigned int pred;
    cin.ignore();
    std::getline(cin, str);
    cout << formatText(str, ) << endl;
}
